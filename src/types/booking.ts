export type Booking = {
  id: string;
  dogName: string;
  breedOrSize: string;
  preferredDate: string;
  preferredTime: string;
  duration: number;
  specialNotes: string;
  isCompleted: boolean;
  createdAt: string;
  updatedAt: string;
};
